@ECHO OFF
:launchstart
SET LAUNCHDIR=C:\LAUNCHER
C:
CD %LAUNCHDIR%
IF EXIST QUIT.TXT DEL QUIT.TXT
launcher.exe > OUT.TXT
IF EXIST QUIT.TXT GOTO end
call %LAUNCHDIR%\run.bat
IF ERRORLEVEL 1 GOTO end
GOTO launchstart
:end